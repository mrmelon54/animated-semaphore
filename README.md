## animated-semaphore

Put your text in the box and press PLAY.

#### Usage as a library

```javascript
var as = new AnimateSemaphore(<jquery element>);
as.init();
as.setText(<text>);
as.showAnimation();
```
